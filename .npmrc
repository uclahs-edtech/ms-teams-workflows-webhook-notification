# npm security hardening

# Prevent installing packages with known vulnerabilities
audit=true

# Do not run lifecycle scripts from dependencies during install
ignore-scripts=false

# Lock to exact versions for reproducible builds
save-exact=true

# Disable funding messages in CI
fund=false